import { ResistanceGameCamera } from './resistance-game-camera';

describe('ResistanceGameCamera', () => {
  it('should create an instance', () => {
    expect(new ResistanceGameCamera()).toBeTruthy();
  });
});
