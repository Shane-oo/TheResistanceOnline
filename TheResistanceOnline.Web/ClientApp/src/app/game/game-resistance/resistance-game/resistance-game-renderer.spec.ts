import { ResistanceGameRenderer } from './resistance-game-renderer';

describe('ResistanceGameRenderer', () => {
  it('should create an instance', () => {
    expect(new ResistanceGameRenderer()).toBeTruthy();
  });
});
