import { MissionLeaderPiece } from './mission-leader-piece';

describe('MissionLeaderPiece', () => {
  it('should create an instance', () => {
    expect(new MissionLeaderPiece()).toBeTruthy();
  });
});
