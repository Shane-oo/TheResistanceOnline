import { MissionSuccessPiece } from './mission-success-piece';

describe('MissionSuccessPiece', () => {
  it('should create an instance', () => {
    expect(new MissionSuccessPiece()).toBeTruthy();
  });
});
